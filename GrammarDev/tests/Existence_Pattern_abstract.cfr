A {
  B ? {}
  `E 5 {
    Y
    [E]
  }
  C ?
  D
    param : int 
  percent -> integer
  
  // Structural constraint
  [ no B ]
  [#C>=5]
  [#A > 10 && #A< 5]

  // Behavioural constraint
  // Existence Pattern
  
  //[ #A > 1 && C is true globally]
  
  //[ D is true globally]

  //[ D is true]
  
  [ V <= 5 is true before D.param < 25]
  
  [ V >= 5  && V <= 8 is true between D.param >= 25 && D.param <= 50]
  
  [ V > 8 is true after D.param > 50 ]
}
